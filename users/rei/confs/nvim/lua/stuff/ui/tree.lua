return {
    close_if_last_window = true,

    filesystem = {
        bind_to_cwd = false,
        follow_current_file = true,
    },
    window = {
        width = 40,
        mappings = {
            ['<space>'] = 'none',
        },
    },
}
